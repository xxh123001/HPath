# 标注配准功能 - 快速开始 🚀

## 🎯 你想做什么？

**问题**: 标注和图像对不上（位置错了、大小不对、旋转了）

**解决**: 用这个功能！标记几个对应点，自动对齐所有标注

---

## ⚡️ 3分钟快速开始

### 第1步: 编译（如果还没编译）

```bash
cd /Users/felix/Downloads/HPath-main/qupath-0.6.0
./gradlew build
```

等待编译完成...

---

### 第2步: 运行HPath

```bash
# 方式1: 命令行
./build/scripts/HPath

# 方式2: 如果已经打包
open build/dist/HPath-1.0.0-arm64.app
```

---

### 第3步: 打开图像

1. 打开一个有标注的图像
2. 确认标注没有对齐

---

### 第4步: 启动功能

```
菜单: Objects → Annotations → Register annotations...
```

会弹出控制面板：

```
┌──────────────────────────────┐
│ Annotation Registration      │
├──────────────────────────────┤
│ [  Add Image Point  ]        │
│ [  Add Annotation Point  ]   │
│ [  Clear All Points  ]       │
│ ☑ Use uniform scaling        │ ← 默认勾选，防止扭曲！
│   (no distortion)            │
│ [  Apply Transform  ]        │
│ [  Cancel  ]                 │
└──────────────────────────────┘
```

**重要**: "Use uniform scaling" 默认勾选，这样标注**不会被扭曲**！

---

### 第5步: 标记第1对点

1. **点击** `Add Image Point` 按钮
2. **在图像上点击** 一个明显的特征点（如血管交叉）
   - 会出现蓝色圆圈 ●

3. **点击** `Add Annotation Point` 按钮  
4. **在标注上点击** 对应的点
   - 会出现红色圆圈 ●
   - 蓝红两点之间有绿色连线

---

### 第6步: 继续标记更多点

重复第5步，再标记1-3对点

**建议**: 
- 等比缩放模式：最少**2对点**，建议**3-4对**
- 如果取消勾选等比缩放：最少**3对点**，建议**4-6对**
- 点要**分散**（如四个角）

---

### 第7步: 应用变换

1. 点击 `Apply Transform` 按钮
2. 弹出确认对话框
3. 点击 `Yes`

**完成！** 🎉

所有标注现在已经对齐了！

---

## 🎨 可视化说明

标记过程中你会看到：

```
图像视图:
┌────────────────────────┐
│ ●1 (蓝) ──── ●1 (红)  │ ← 第1对点
│   │          │        │
│   │          │        │
│ ●2 (蓝) ──── ●2 (红)  │ ← 第2对点
│   │          │        │
│ ●3 (蓝) ──── ●3 (红)  │ ← 第3对点
│                        │
│ ●4 (蓝) ──── ●4 (红)  │ ← 第4对点
└────────────────────────┘

蓝色 = 图像上的点
红色 = 标注上的点
绿线 = 对应关系
```

---

## 💡 重要提示

### ✅ 做这些:
- **保持勾选"等比缩放"**（防止扭曲！）
- 选择**明显的特征点**（角点、交叉点）
- 点要**分散分布**（不要都在一起）
- 标记至少 **2对点**（推荐3-4对）
- **仔细点击**（精度很重要）

### ❌ 不要这样:
- 取消勾选等比缩放（除非真需要）
- 所有点都在一个小区域
- 所有点在一条直线上
- 点击太随意不精确
- 少于2对点

---

## 🆘 遇到问题？

### 标注被扭曲变形了！
→ **确保勾选了"Use uniform scaling (no distortion)"**
→ 这是最常见的问题！

### 提示 "需要至少2对点"（或3对）
→ 继续标记控制点
→ 勾选等比缩放：至少2对
→ 不勾选：至少3对

### 提示 "点可能共线"
→ 点都在一条线上了，重新选分散的点

### 变换后还是不对
→ 确认勾选了等比缩放
→ 清除所有点，重新仔细标记
→ 增加控制点数量

### 按钮是灰的
→ 按照步骤来：先加图像点，再加标注点，交替进行

---

## 📖 更多帮助

### 想看详细教程？
→ 阅读 `使用流程说明.md`

### 想了解原理？
→ 阅读 `标注配准功能说明.md`

### 想练习一下？
→ 运行 `test-registration.groovy` 脚本

### 所有文档在哪？
→ 查看 `标注配准功能文档索引.md`

---

## 🎯 典型场景

### 场景1: 标注偏移了
```
标记3-4个点 → 应用 → 标注移到正确位置
```

### 场景2: 标注太大或太小
```
标记4个角点 → 应用 → 标注缩放到正确大小
```

### 场景3: 标注旋转了
```
标记4个明显特征点 → 应用 → 标注旋转到正确角度
```

---

## ✨ 就是这么简单！

**7个步骤，3分钟，搞定配准！**

现在去试试吧！ 🚀

---

## 📞 联系

遇到问题？
1. 查看日志: `Help → Show log`
2. 阅读文档: `标注配准功能文档索引.md`
3. 运行测试: `test-registration.groovy`

---

**祝配准成功！** 🎊

