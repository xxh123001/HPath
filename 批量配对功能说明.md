# 批量配对功能使用说明

## 功能概述

**批量配对功能**允许你通过正则表达式自动匹配大量的图像文件和标注文件，并批量创建配对结果。非常适合处理文件名一致但后缀不同的场景（如 `image1.svs` 和 `image1.json`）。

---

## 使用场景

**典型场景：**
- 你有 100 个 `.svs` 图像文件
- 你有 100 个 `.json` 标注文件  
- 文件名一致：`K2024-001.svs` 对应 `K2024-001.json`
- 需要批量配对并合并

---

## 如何使用

### 1. 准备工作

1. 确保已经导入图像到Project
2. 导入所有JSON标注文件到Objects列表

### 2. 打开批量配对对话框

点击 **"🔗 Batch Merge by Name"** 按钮

### 3. 配置正则表达式

对话框会显示两个输入框：

#### 左侧：Image Name Pattern (图像名称模式)

**默认值：** `(.+)\.(svs|tif|tiff|ndpi|jpg)$`

**含义：**
- `(.+)` - 捕获组1：提取文件名主体部分
- `\.` - 匹配点号 `.`
- `(svs|tif|tiff|ndpi|jpg)` - 匹配图像文件扩展名
- `$` - 字符串结尾

**示例：**
```
输入文件名: K2024-4538.svs
提取结果: K2024-4538
```

#### 右侧：Object Name Pattern (对象名称模式)

**默认值：** `(.+)\.(json|geojson)$`

**含义：**
- `(.+)` - 捕获组1：提取文件名主体部分
- `\.` - 匹配点号 `.`
- `(json|geojson)` - 匹配标注文件扩展名
- `$` - 字符串结尾

**示例：**
```
输入文件名: K2024-4538.json
提取结果: K2024-4538
```

### 4. 查看匹配结果

对话框分为三个区域：

#### 上半部分：匹配的文件列表

- **左侧：** 显示所有匹配的图像及其提取的名称
  ```
  [K2024-4538] K2024-4538.svs
  [K2024-4674] K2024-4674.ndpi
  [2015-2254] 2015-2254.tif
  ```

- **右侧：** 显示所有匹配的对象文件及其提取的名称
  ```
  [K2024-4538] K2024-4538.json
  [K2024-4674] K2024-4674.json
  [2015-2254] 2015-2254.geojson
  ```

#### 下半部分：配对结果

显示成功配对的项目：
```
✓ K2024-4538: K2024-4538.svs + K2024-4538.json
✓ K2024-4674: K2024-4674.ndpi + K2024-4674.json
✓ 2015-2254: 2015-2254.tif + 2015-2254.geojson
```

标题会显示配对数量：`Paired Results (3 pairs)`

### 5. 执行批量合并

- 确认配对结果正确
- 点击 **OK** 按钮
- 所有配对会自动添加到 **"Merged Results"** 列表
- 可以在Merged Results中双击打开任意配对

---

## 正则表达式示例

### 示例1：基本文件名匹配

**场景：** 文件名格式为 `文件名.扩展名`

```regex
图像: (.+)\.(svs|tif)$
对象: (.+)\.json$
```

**匹配：**
- `sample1.svs` + `sample1.json` → ✓
- `test_image.tif` + `test_image.json` → ✓

---

### 示例2：带下划线的文件名

**场景：** 文件名包含下划线分隔符

```regex
图像: (.+_\d+)\.(svs)$
对象: (.+_\d+)\.json$
```

**匹配：**
- `patient_001.svs` + `patient_001.json` → ✓
- `slide_2024_123.svs` + `slide_2024_123.json` → ✓

---

### 示例3：只提取编号部分

**场景：** 文件名格式为 `prefix-编号.扩展名`，只用编号配对

```regex
图像: K\d{4}-(\d+).*\.svs$
对象: K\d{4}-(\d+).*\.json$
```

**匹配：**
- `K2024-4538_slide1.svs` + `K2024-4538_anno.json` → ✓ (都提取到4538)
- `K2024-4674_final.svs` + `K2024-4674_labels.json` → ✓ (都提取到4674)

---

### 示例4：复杂命名规则

**场景：** 文件名格式为 `日期-患者编号-切片编号.扩展名`

```regex
图像: (\d{4}-\d{4}-\d+).*\.(svs|ndpi)$
对象: (\d{4}-\d{4}-\d+).*\.json$
```

**匹配：**
- `2024-1234-001_H&E.svs` + `2024-1234-001_annotations.json` → ✓
- `2024-5678-002.ndpi` + `2024-5678-002.json` → ✓

---

## 常见问题

### Q1: 为什么没有配对成功？

**A:** 检查以下几点：
1. **提取的名称是否完全一致** - 查看匹配列表中的 `[提取名称]`
2. **正则表达式是否正确** - 确保捕获组 `(...)` 提取了正确的部分
3. **大小写是否匹配** - 正则表达式默认区分大小写

### Q2: 如何测试正则表达式？

**A:** 
1. 修改正则表达式
2. 按 **Enter** 键或点击 **"🔍 Match by Name"** 按钮
3. 查看匹配结果实时更新

### Q3: 一个图像可以匹配多个对象吗？

**A:** 
当前版本是**一对一配对**。如果一个图像名称匹配到多个对象，只会配对第一个匹配的对象。

### Q4: 配对后可以修改吗？

**A:**
可以！配对结果会添加到 **Merged Results** 列表，你可以：
- 选择单个或多个结果删除（Remove Selected）
- 清空所有结果（Clear All）
- 重新运行批量配对

### Q5: 正则表达式错误怎么办？

**A:**
如果输入的正则表达式语法错误，系统会弹出错误提示并显示错误信息。

---

## 快捷操作

- **Enter键** - 在正则输入框中按Enter快速执行匹配
- **Ctrl+A** - 在列表中全选
- **双击** - 在Merged Results中双击打开配对

---

## 高级技巧

### 技巧1：排除特定文件

```regex
图像: (?!.*_thumb)(.+)\.(svs)$
对象: (?!.*_backup)(.+)\.json$
```

这会排除文件名中包含 `_thumb` 的图像和包含 `_backup` 的对象文件。

### 技巧2：只匹配特定前缀

```regex
图像: (K2024-\d+).*\.svs$
对象: (K2024-\d+).*\.json$
```

只匹配以 `K2024-` 开头的文件。

### 技巧3：忽略大小写

在正则表达式前添加 `(?i)`:

```regex
图像: (?i)(.+)\.(SVS|svs|TIF|tif)$
对象: (?i)(.+)\.(JSON|json)$
```

---

## 注意事项

1. **确保正则表达式有捕获组** - 使用 `(...)` 括号
2. **捕获组1** 会被用作匹配的键
3. **配对是基于字符串精确匹配** - 提取的名称必须完全相同
4. **建议先测试** - 在小数据集上测试正则表达式是否正确

---

## 完整工作流程

1. **导入数据**
   - Project → Import Images
   - Objects → Import Objects

2. **打开批量配对**
   - 点击 "🔗 Batch Merge by Name"

3. **调整正则**
   - 根据你的文件命名规则调整正则表达式
   - 按Enter查看匹配结果

4. **检查配对**
   - 查看 "Paired Results" 区域
   - 确认配对正确

5. **执行合并**
   - 点击 OK
   - 查看 Merged Results

6. **使用结果**
   - 双击打开任意配对
   - 或批量处理

---

## 更新日期

**2025-10-27**

批量配对功能已完成开发和测试，立即可用！🎉

