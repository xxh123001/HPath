# 导入后检查清单

如果导入后看不到标注，请按以下步骤检查：

## 1. 检查对象是否真的导入了

### 方法A: 查看对象列表
- 打开 `View → Objects` 或按快捷键
- 查看是否有新的对象（应该有1000个 detection 对象）

### 方法B: 查看日志
在日志窗口查找：
```
Successfully parsed XXX PathObjects from GeoJSON
```

## 2. 如果对象列表中有对象但看不见

### 原因1: 对象在图像外（坐标不匹配）
**检查**：
1. 你的图像大小是多少？
2. GeoJSON 中的坐标范围：约 10000-12000

**解决**：
- 如果图像小于 12000 x 12000 像素，对象会在图像外！
- 需要使用大尺寸的图像，或缩放对象坐标

### 原因2: 对象太小
**解决**：缩小视图（Zoom Out）

### 原因3: 对象是 detection 而不是 annotation
**解决**：
1. 确保显示 detection 对象：`View → Show Detections` 或按 `D` 键
2. 检查对象颜色设置

## 3. 快速验证命令

在终端中检查你的图像大小和GeoJSON坐标范围：

```bash
# 检查 GeoJSON 坐标范围
cat "/Users/felix/Library/Containers/com.tencent.xinWeChat/Data/Documents/xwechat_files/wxid_kjh0q6eeyhhv12_628b/msg/file/2025-10/annotation_batch_002_of_129.geojson" | python3 -c "
import json, sys
data = json.load(sys.stdin)
coords = []
for f in data['features']:
    for ring in f['geometry']['coordinates']:
        for point in ring:
            coords.append(point)
xs = [p[0] for p in coords]
ys = [p[1] for p in coords]
print(f'X range: {min(xs)} - {max(xs)}')
print(f'Y range: {min(ys)} - {max(ys)}')
"
```

## 4. 在 HPath 中查看图像信息

1. 打开图像后，查看窗口底部状态栏
2. 应该显示图像大小（宽 x 高）
3. 如果图像是 3000 x 2000，但标注坐标在 10000-12000，那就看不见

## 5. 解决方案

如果坐标不匹配，有两个选择：

### 选项A: 缩放GeoJSON坐标
创建一个Python脚本缩放坐标：
```python
import json

with open('input.geojson') as f:
    data = json.load(f)

scale = 0.3  # 根据需要调整
for feature in data['features']:
    coords = feature['geometry']['coordinates']
    for ring in coords:
        for point in ring:
            point[0] = int(point[0] * scale)
            point[1] = int(point[1] * scale)

with open('output.geojson', 'w') as f:
    json.dump(data, f)
```

### 选项B: 使用匹配大小的图像
使用宽度至少 12000 像素的大图像

---

**现在请检查并告诉我**：
1. 对象列表中是否有1000个对象？
2. 你的图像大小是多少？
3. 是否显示了 detection 对象（按 `D` 键）？

