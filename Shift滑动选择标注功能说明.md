# 快速批量选择标注功能说明

## 功能概述

新增了**两种**快速批量选择标注的功能：
1. **Shift+滑动选择**：按住Shift键，鼠标滑过的标注自动被选中
2. **Multi-Select按钮**：点击激活持续多选模式，无需按住Ctrl键

## 使用方法

### 方法1：Shift+滑动选择 ⚡

#### 1️⃣ 激活选择模式
- **按住 Shift 键**（不要松开）
- 鼠标光标会变成 **十字准星 ✛**

#### 2️⃣ 滑动选择标注
- 保持 Shift 按下
- **移动鼠标**滑过你想选择的标注
- 每当鼠标滑过一个标注，它会**自动被添加到选中列表**

#### 3️⃣ 批量修改类型
- 松开 Shift 键
- 在菜单中选择：**Objects → Annotations → Set class...**
- 所有被选中的标注会一起修改类型

---

### 方法2：Multi-Select按钮模式 🔘

#### 1️⃣ 找到Multi-Select按钮
- 在**图像查看器右上角**有一个 **Multi-Select** 按钮
- 默认状态：未激活（灰色）

#### 2️⃣ 激活持续多选模式
- **点击 Multi-Select 按钮**
- 按钮变成**高亮/按下状态**
- 现在进入持续多选模式！

#### 3️⃣ 点击选择多个标注
- **直接点击**标注，就像按住Ctrl一样
- 不需要按任何键！
- 想选多少个就点多少个

#### 4️⃣ 批量修改类型
- 选好所有标注后
- 右键 → **Set class...** 
- 所有选中的标注一起修改

#### 5️⃣ 退出多选模式
- **再次点击 Multi-Select 按钮**
- 按钮恢复正常状态
- 回到普通点击模式

## 使用场景

### 场景1：快速批量设置类型（Multi-Select按钮）
```
问题：有20个分散的标注需要从"Tumor"改为"Stroma"

传统方法：
1. Ctrl+点击第1个标注
2. Ctrl+点击第2个标注
3. ... 重复20次！😫 手指累
4. Set class

新方法（Multi-Select按钮）：
1. 点击右上角 Multi-Select 按钮 🔘
2. 点点点...连续点击20个标注 🖱️
3. 再次点击 Multi-Select 按钮（退出模式）
4. Set class  ✅ 完成！
```

### 场景2：连续区域快速选择（Shift+滑动）
```
问题：一片区域的10个标注需要修改

新方法（Shift+滑动）：
1. 按住Shift
2. 像画笔一样滑过这些标注 🖱️✨
3. 松开Shift
4. Set class  ✅ 超快！
```

### 两种方法的选择

| 场景 | 推荐方法 | 原因 |
|------|---------|------|
| 标注分散在各处 | Multi-Select按钮 | 可以慢慢点，不用一直按键 |
| 标注在连续区域 | Shift+滑动 | 快速滑过即可，最快 |
| 需要精确选择 | Multi-Select按钮 | 可以仔细确认每个 |
| 大量标注 | Shift+滑动 | 像画笔一样扫过 |

## 功能特点

### ✅ 优点
- **快速**：不用一个个点击
- **流畅**：鼠标滑过即可
- **直观**：看到标注就选中
- **批量**：支持多选

### ⚙️ 技术细节
- 只会选择 **标注（Annotations）**，不会选择检测对象
- 使用**多选模式**，不会清除之前的选择
- 重复滑过同一个标注不会重复添加
- 鼠标光标变为十字准星，提示当前处于选择模式

## 所有选择方式对比

| 方式 | 功能 | 需要按键 | 持续性 | 适用场景 |
|------|------|---------|---------|---------|
| **单击** | 选中单个 | 无 | - | 选择单个对象 |
| **Ctrl+点击** | 多选 | 需要一直按Ctrl | 按着才有效 | 少量多选 |
| **Multi-Select按钮** 🆕 | 持续多选 | 无 | 持续有效 | 大量分散的对象 |
| **Shift+滑动** 🆕 | 滑动多选 | 需要一直按Shift | 按着才有效 | 连续区域的对象 |
| **Alt+点击** | 选择并缩放 | 需要按Alt | - | 快速定位对象 |

## 注意事项

1. **只对标注有效**
   - ✅ 适用于：Annotations（标注）
   - ❌ 不适用于：Detections（检测对象）

2. **保持Shift按下**
   - 必须持续按住Shift键
   - 松开后恢复正常鼠标模式

3. **不会取消选择**
   - 只会添加到选择列表
   - 要取消选择，点击空白区域或使用"Clear selection"

## 完整工作流示例

### 示例A：批量设置50个标注为"Immune"类型（分散分布）

**传统方式**：需要50次Ctrl+点击 😫

**新方式（Multi-Select按钮）**：
1. 确保使用 **Move工具**（手型图标）
2. **点击右上角 Multi-Select 按钮**（激活）
3. **连续点击50个标注**（不需要按任何键！）
4. **再次点击 Multi-Select 按钮**（关闭）
5. 右键 → Set class → Immune
6. ✅ 完成！

### 示例B：批量设置一片区域的30个标注（连续区域）

**新方式（Shift+滑动）**：
1. 确保使用 **Move工具**
2. **按住Shift键**
3. **快速滑动鼠标**穿过这30个标注
4. 看到标注被逐个高亮选中
5. **松开Shift**
6. 右键 → Set class → Stroma
7. ✅ 超快！只需3秒

## 修改的文件

1. `qupath-0.6.0/qupath-gui-fx/src/main/java/qupath/lib/gui/viewer/tools/handlers/MoveToolEventHandler.java`
   - 添加了Shift+滑动选择逻辑
   - 添加了Multi-Select模式属性和逻辑
   
2. `qupath-0.6.0/qupath-gui-fx/src/main/java/qupath/lib/gui/viewer/QuPathViewer.java`
   - 在图像查看器右上角添加了Multi-Select切换按钮

## Multi-Select按钮位置

按钮位于**图像查看器的右上角**，浮动在图像上方：

```
┌────────────────────────────────────┐
│                    [Multi-Select]  │ ← 右上角
│                                    │
│        [图像显示区域]              │
│                                    │
│                                    │
└────────────────────────────────────┘
```

## 视觉反馈

### Multi-Select按钮状态
- **未激活**：按钮显示为普通按钮样式，文字 "Multi-Select"
- **激活中**：按钮高亮/按下状态，表示多选模式已启用
- **提示**：鼠标悬停在按钮上会显示功能说明

### Shift+滑动模式
- **激活时**：鼠标光标变为十字准星 ✛
- **未激活**：鼠标恢复为手型（Move工具）

## 日期
2025-11-02

