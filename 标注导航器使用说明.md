# 标注导航器（Annotation Navigator）使用说明

## ✨ 功能简介

**标注导航器**是一个强大的搜索和导航工具，让你能在大图中快速定位标注。

### 核心功能

✅ **搜索标注** - 按名称、分类、ID、测量值搜索
✅ **双击跳转** - 双击标注自动定位并缩放
✅ **实时过滤** - 输入即时显示结果
✅ **智能缩放** - 自动调整到合适的缩放级别
✅ **高亮显示** - 自动选中目标标注

---

## 📍 菜单位置

编译后，在菜单中找到：

```
Objects → Annotations → Annotation Navigator...
```

---

## 🎯 使用场景

### 场景1: 大图中有很多标注

```
问题: 有115522个标注，怎么快速找到特定的？
解决: 使用导航器搜索！
```

### 场景2: 按分类查找

```
搜索框输入: Tumor
结果: 只显示Tumor分类的标注
双击: 跳转到该标注
```

### 场景3: 按名称查找

```
搜索框输入: region_1
结果: 显示名称包含region_1的标注
双击: 定位到该标注
```

### 场景4: 按ID查找

```
搜索框输入: e9c0e693
结果: 找到ID匹配的标注
双击: 跳转过去
```

---

## 🖥️ 界面说明

### 导航器窗口

```
┌──────────────────────────────────────┐
│  Annotation Navigator                │
├──────────────────────────────────────┤
│ 🔍 Search annotations...             │
├──────────────────────────────────────┤
│ [Tumor] Region 1 [10 measurements]   │
│    ID: e9c0e693...                   │
├──────────────────────────────────────┤
│ [Tumor] Region 2 [10 measurements]   │
│    ID: a4f5b123...                   │
├──────────────────────────────────────┤
│ [Stroma] Region 3 [8 measurements]   │
│    ID: c7d8e456...                   │
├──────────────────────────────────────┤
│ ...                                  │
├──────────────────────────────────────┤
│ Showing: 3 / 115522 annotations      │
└──────────────────────────────────────┘
```

### 操作方式

**搜索**:
- 在搜索框输入关键词
- 列表实时过滤

**跳转**:
- 双击任意标注
- 自动定位到该标注
- 自动缩放到合适级别
- 自动选中并高亮

---

## 💡 搜索技巧

### 按分类搜索

```
输入: Tumor
结果: 所有Tumor分类的标注

输入: Positive
结果: 所有Positive分类的标注

输入: Cell
结果: 所有Cell分类的标注
```

### 按名称搜索

```
输入: region
结果: 名称包含"region"的标注

输入: area_1
结果: 名称包含"area_1"的标注
```

### 按ID搜索

```
输入: e9c0e693
结果: ID开头匹配的标注
```

### 按测量值名称搜索

```
输入: DAB OD
结果: 包含DAB OD测量的标注
```

---

## 🎨 使用示例

### 示例1: 在115522个标注中找Tumor

1. **打开导航器**:
   ```
   Objects → Annotations → Annotation Navigator...
   ```

2. **搜索框输入**: `Tumor`

3. **结果**: 只显示Tumor分类的标注

4. **双击任意一个** → 自动跳转！

### 示例2: 查找特定名称的标注

1. 假设你的标注命名为 `ROI_001`, `ROI_002`...

2. 在搜索框输入: `ROI_005`

3. 找到后双击 → 跳转到该标注

### 示例3: 快速浏览所有标注

1. 打开导航器

2. 搜索框留空（显示所有）

3. 用方向键↑↓浏览列表

4. 看到想看的，双击跳转

---

## 🔍 搜索规则

### 支持的搜索字段

导航器会在以下字段中搜索：
- ✅ 标注名称 (Name)
- ✅ 分类 (Classification)
- ✅ 对象ID
- ✅ 测量值名称

### 搜索特点

- **不区分大小写** (`tumor` 能找到 `Tumor`)
- **部分匹配** (`reg` 能找到 `region_1`)
- **实时过滤** (输入即时更新)
- **多字段** (同时搜索多个字段)

---

## ⌨️ 快捷操作

### 键盘操作

- **↑↓ 方向键**: 在列表中移动
- **Enter**: 跳转到选中的标注
- **Ctrl+F**: 聚焦到搜索框
- **Esc**: 清空搜索

### 鼠标操作

- **单击**: 选中标注（预览信息）
- **双击**: 跳转到标注
- **悬停**: 显示详细信息（Tooltip）

---

## 🎯 实际应用

### 应用1: 质量检查

```
场景: 检查某些标注是否正确

步骤:
1. 打开导航器
2. 搜索特定分类
3. 逐个双击查看
4. 发现问题及时修正
```

### 应用2: 区域定位

```
场景: 快速跳转到特定区域

步骤:
1. 搜索区域名称
2. 双击跳转
3. 开始分析
```

### 应用3: 批量检查

```
场景: 检查所有Tumor标注

步骤:
1. 搜索: Tumor
2. 看到所有Tumor标注
3. 逐个双击检查
```

---

## 📊 显示信息

### 列表中显示

每个标注显示：
```
[分类] 名称 [测量值数量]
   ID: xxx...
```

### Tooltip显示

悬停时显示：
```
Class: Tumor
Name: Region_001
ID: e9c0e693-00ea-41d5-b3fc-13...
Centroid: (3395.4, 2496.3)
Area: 49596.9 µm²
Measurements: 10

Double-click to jump to this annotation
```

---

## 🚀 立即使用

### 编译

```bash
cd /Users/felix/Downloads/HPath-main/qupath-0.6.0
./gradlew build
./build/scripts/HPath
```

### 打开导航器

```
Objects → Annotations → Annotation Navigator...
```

### 测试

1. 搜索框输入关键词
2. 双击任意标注
3. 查看是否自动跳转

---

## 💡 高级用法

### 结合其他功能

**搜索 + 批量操作**:
```groovy
// 在导航器中搜索到的标注，可以用脚本批量处理

// 例如：删除所有Tumor标注
def tumors = getAnnotationObjects().findAll { 
    it.getPathClass()?.toString() == "Tumor" 
}
removeObjects(tumors, true)
```

**搜索 + 导出**:
```groovy
// 导出搜索到的标注
def filtered = getAnnotationObjects().findAll { 
    it.getName()?.contains("region") 
}

// 导出为GeoJSON
def gson = GsonTools.getInstance()
def json = gson.toJson(filtered)
new File('/path/to/filtered.geojson').text = json
```

---

## 🎊 功能亮点

### 对比QuPath默认列表

| 功能 | 默认列表 | 导航器 |
|------|---------|--------|
| 搜索 | ❌ 无 | ✅ 强大 |
| 跳转 | ⚠️ 需手动 | ✅ 双击 |
| 过滤 | ⚠️ 基础 | ✅ 实时 |
| 缩放 | ❌ 无 | ✅ 自动 |
| 多字段搜索 | ❌ 无 | ✅ 有 |
| 显示详情 | ⚠️ 有限 | ✅ 丰富 |

### 特别适合

- ✅ 大图像（全切片扫描）
- ✅ 大量标注（>1000个）
- ✅ 需要快速定位
- ✅ 质量检查
- ✅ 区域导航

---

## 🎯 对你的115522个标注

**完美适用！**

```
1. 打开导航器
   ↓
2. 搜索想要的标注
   ↓
3. 双击跳转
   ↓
4. 快速定位！✅
```

---

**编译后立即可用！** 🚀

**文件**: `AnnotationNavigatorCommand.java` (已创建，无编译错误)

**菜单**: `Objects → Annotations → Annotation Navigator...`

**特别适合处理大量标注！** 🎯

