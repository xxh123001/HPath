# 实时阈值调整工具使用说明

## 📋 功能概述

这是一个交互式阈值调整工具，可以将Python的 `interactive_threshold_adjuster.py` 功能转换为QuPath中的实时交互工具。

### 主要功能：

1. **实时阈值调整** - 通过滑动条或输入框调整阈值
2. **实时预览** - 点击"实时预览"按钮立即查看分类效果
3. **自动统计** - 自动计算光密度分布和分类统计
4. **支持多种测量参数** - 支持所有光密度测量参数（DAB OD mean, Nucleus OD mean等）
5. **灵活分类** - 可自定义阳性/阴性类别名称

---

## 🚀 使用步骤

### 1. 准备工作

确保你的图像已经：
- ✅ 运行过细胞检测或对象检测
- ✅ 计算了光密度测量值（OD measurements）
- ✅ 图像已打开并显示在QuPath中

### 2. 运行脚本

1. 打开QuPath/HPath
2. 打开包含检测对象的图像
3. 菜单：`Script → Run → 选择 "实时阈值调整-交互式.groovy"`
4. 或者直接在脚本编辑器中运行

### 3. 使用界面

#### 界面元素说明：

1. **光密度测量参数** - 下拉菜单选择要使用的测量参数
   - 例如：`Detection: DAB OD mean`, `Cell: DAB OD mean`, `Nucleus: DAB OD mean` 等

2. **阈值调整**
   - **滑动条** - 拖动调整阈值（快速预览范围）
   - **输入框** - 精确输入阈值数值

3. **类别设置**
   - **阳性类别** - 高于阈值对象的分类名称（默认：`Positive`）
   - **阴性类别** - 低于阈值对象的分类名称（默认：`Negative`）

4. **统计信息** - 显示光密度分布统计和推荐阈值

5. **实时预览区域** - 显示当前阈值下的分类统计

---

## 🎯 操作流程

### 方式1：快速调整（推荐）

1. **查看统计信息** - 脚本会自动分析光密度分布，显示：
   - 平均值、中位数、百分位数
   - 推荐阈值（保守/中等/宽松）

2. **调整阈值**
   - 拖动滑动条快速调整
   - 或直接在输入框中输入精确值

3. **实时预览**
   - 点击 **"🔍 实时预览"** 按钮
   - 对象分类会立即更新到视图中
   - 可以继续调整阈值并预览

4. **确认应用**
   - 满意预览结果后，点击 **"✅ 确认应用"**
   - 分类会被保存

### 方式2：精确调整

1. **选择测量参数** - 从下拉菜单选择合适的光密度测量参数
2. **输入阈值** - 在输入框中直接输入阈值（例如：`0.25`）
3. **预览效果** - 点击"实时预览"查看
4. **微调** - 根据预览结果微调阈值
5. **确认** - 点击"确认应用"

---

## 📊 测量参数说明

### 常见的测量参数名称

| 参数名称 | 说明 | 适用对象 |
|---------|------|---------|
| `Detection: DAB OD mean` | DAB光密度平均值 | 检测对象 |
| `Cell: DAB OD mean` | 细胞DAB光密度平均值 | 细胞 |
| `Nucleus: DAB OD mean` | 细胞核DAB光密度平均值 | 细胞 |
| `Cytoplasm: DAB OD mean` | 细胞质DAB光密度平均值 | 细胞 |
| `Detection: Hematoxylin OD mean` | Hematoxylin光密度平均值 | 检测对象 |

### 如何查看可用的测量参数？

脚本会自动检测并显示所有包含 "OD" 和 "mean" 的测量参数。

你也可以：
1. 选择任意对象
2. `Measure → Show measurements`
3. 查看所有测量参数名称

---

## 💡 使用技巧

### 1. 阈值选择建议

根据统计信息中的建议：
- **保守阈值**（75百分位）- 只有高亮度对象被分类为阳性
- **中等阈值**（中位数）- 平衡阳性/阴性比例
- **宽松阈值**（25百分位）- 更多对象被分类为阳性

### 2. 多次预览

- 可以多次点击"实时预览"来查看不同阈值的效果
- 预览会立即更新视图，无需等待

### 3. 组合使用

- 可以先用脚本调整到一个粗略的阈值
- 然后使用QuPath的分类器进行进一步细化

### 4. 保存结果

分类结果会自动保存到图像数据中，可以：
- `File → Save` 保存项目
- `File → Export objects → Export as GeoJSON` 导出结果

---

## 🔍 与Python版本的对比

### Python版本 (`interactive_threshold_adjuster.py`)
- ✅ 命令行交互
- ✅ 需要预先计算缓存文件
- ✅ 生成新的GeoJSON文件
- ❌ 需要手动导入到QuPath

### Groovy版本 (`实时阈值调整-交互式.groovy`)
- ✅ 图形界面交互
- ✅ 实时预览效果
- ✅ 直接在当前图像中应用
- ✅ 无需手动导入
- ✅ 滑动条快速调整

---

## ⚠️ 常见问题

### Q1: 提示"没有光密度测量值"

**原因**：对象检测时未启用光密度计算

**解决**：
1. 重新运行检测，确保启用光密度测量
2. 或在检测后运行添加光密度测量的脚本

### Q2: 预览按钮没有反应

**检查**：
1. 阈值输入是否有效（数字格式）
2. 图像是否有检测对象
3. 查看QuPath控制台的错误信息

### Q3: 分类后看不到颜色变化

**检查**：
1. `View → Show/hide` 确保显示对象分类颜色
2. `Edit → Preferences → Objects` 检查颜色设置

### Q4: 如何撤销分类？

**方法**：
1. `Edit → Undo` 撤销最后一次操作
2. 或重新运行脚本，设置不同的阈值

---

## 📝 示例工作流

### 完整工作流示例：

1. **检测细胞**
   ```
   运行 "传统细胞检测-带光密度.groovy"
   或 "阳性细胞检测-完整版.groovy"
   ```

2. **调整阈值**
   ```
   运行 "实时阈值调整-交互式.groovy"
   使用滑动条调整阈值
   点击"实时预览"查看效果
   点击"确认应用"
   ```

3. **导出结果**
   ```
   File → Export objects → Export as GeoJSON
   ```

---

## 🎨 界面预览

```
┌─────────────────────────────────────────┐
│  🎛️ 实时阈值调整                        │
├─────────────────────────────────────────┤
│  光密度测量参数: [Detection: DAB OD mean]│
│                                          │
│  阈值:  [━━━━━━━━━━━━━━━━━━━━━━━━━━━━] │
│         [0.25_________________________]  │
│                                          │
│  阳性类别: [Positive]                   │
│  阴性类别: [Negative]                   │
│                                          │
│  统计信息:                               │
│  - 总对象数: 1234                        │
│  - 平均值: 0.23                          │
│  - 中位数: 0.20                          │
│  - 建议阈值: 保守/中等/宽松             │
│                                          │
│  实时预览:                               │
│  [🔍 实时预览]                           │
│  当前阈值: 0.25                          │
│  - 阳性对象: 567 (45.9%)                │
│  - 阴性对象: 667 (54.1%)                │
│                                          │
│           [取消] [🔍 实时预览] [✅ 确认应用]│
└─────────────────────────────────────────┘
```

---

## 📚 相关脚本

- `传统细胞检测-带光密度.groovy` - 运行检测并计算光密度
- `阳性细胞检测-完整版.groovy` - 完整检测和分类流程
- `光密度/` 文件夹 - Python版本的阈值调整工具

---

## 🆘 需要帮助？

如果遇到问题：
1. 查看QuPath控制台的输出信息
2. 检查图像是否有检测对象和光密度测量
3. 参考 `光密度JSON参数说明.md` 了解测量参数

---

**享受实时阈值调整的便利！** 🎉

