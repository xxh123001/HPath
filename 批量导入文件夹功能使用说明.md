# 批量导入文件夹功能使用说明

## 功能概述

**批量导入文件夹功能**允许你一次性导入整个文件夹结构，每个子文件夹作为一个分组，自动导入其中的所有图像和JSON标注文件，并在左侧显示树形结构。

---

## 适用场景

### 典型的文件夹结构

```
kt_merged_split/
├── K2023-0460/
│   ├── light.svs
│   ├── kt.json
│   ├── qp1_480_all.svs
│   ├── qp1_520_yuan.svs
│   └── ...
├── K2023-1974/
│   ├── light.svs
│   ├── kt.json
│   ├── qp2_480_xueguan.svs
│   └── ...
├── K2023-2593/
│   ├── light.svs
│   ├── kt.json
│   └── ...
└── ...
```

---

## 使用步骤

### 1. 创建或打开Project

首先确保已经创建了一个Project。

### 2. 点击批量导入按钮

在左侧Images区域，点击 **"📁 Batch Import Folder"** 按钮

### 3. 选择根文件夹

在弹出的文件夹选择器中，选择包含所有子文件夹的根目录（例如：`kt_merged_split`）

### 4. 确认导入

系统会扫描并显示：
```
Found 96 subfolder(s).

Each subfolder will be imported as a group.
Images and JSON files will be automatically paired.

Continue?
```

点击 **Yes** 继续。

### 5. 自动导入

系统会自动：
- ✅ 扫描每个子文件夹
- ✅ 导入所有图像文件（.svs, .tif, .ndpi, 等）
- ✅ 导入所有JSON标注文件（.json, .geojson）
- ✅ 为每个图像添加"Folder"元数据
- ✅ 自动按文件夹分组显示

### 6. 查看结果

导入完成后：

**左侧Images树形结构：**
```
📁 1/project.qpproj (846)
  📁 K2023-0460 (9)
    - light.svs
    - qp1_480_all.svs
    - qp1_520_yuan.svs
    - qp1_620_jin.svs
    ...
  📁 K2023-1974 (10)
    - light.svs
    - qp2_480_xueguan.svs
    ...
  📁 K2023-2593 (8)
    - light.svs
    ...
```

**右侧Objects树形结构：**
```
📁 K2023-0460 (1)
  - kt.json (2205 objects)
📁 K2023-1974 (1)
  - kt.json (1783 objects)
📁 K2023-2593 (1)
  - kt.json (1077 objects)
...
```

---

## 支持的文件类型

### 图像文件
- `.svs` - Aperio格式
- `.tif`, `.tiff` - TIFF格式
- `.ndpi` - Hamamatsu格式
- `.vsi` - Olympus格式
- `.mrxs` - MIRAX格式
- `.scn` - Leica格式
- `.jpg`, `.png` - 标准图像格式

### 标注文件
- `.json` - 标准JSON格式
- `.geojson` - GeoJSON格式

---

## 树形结构特性

### 自动分组
- **Images** - 按"Folder"元数据自动分组
- **Objects** - 按导入时的文件夹路径自动分组

### 可折叠展开
- 点击 **▼** 折叠文件夹
- 点击 **▶** 展开文件夹
- 所有文件夹默认展开状态

### 选择操作
- **单击** - 选中单个文件
- **Shift+点击** - 范围选择
- **Ctrl/Cmd+点击** - 添加到顺序选择（用于配对）

---

## 批量配对工作流程

### 完整流程示例

1. **批量导入**
   ```
   📁 Batch Import Folder → 选择 kt_merged_split
   ```

2. **查看树形结构**
   - Images按文件夹分组显示
   - Objects按文件夹分组显示

3. **批量配对**
   ```
   🔗 Batch Merge by Name
   ```
   - Image Pattern: `(.+)\.(svs|tif)$`
   - Object Pattern: `(.+)\.json$`
   - 点击 Match → 自动配对

4. **查看结果**
   ```
   Paired Results (96 pairs)
   ✓ K2023-0460: light.svs + kt.json
   ✓ K2023-1974: light.svs + kt.json
   ...
   ```

5. **执行合并**
   - 点击 OK
   - 所有配对添加到 Merged Results

---

## 高级功能

### 文件名显示
- 现在图像会显示实际的文件名（如 `light.svs`, `qp1_480_all.svs`）
- 不再显示"Image null"

### 元数据管理
- 每个导入的图像都会自动添加"Folder"元数据
- 可以在Project中使用 **Sort by → Folder** 查看树形结构

### 可调节布局
- **左右分隔条** - 拖动调节Images和Objects区域宽度
- **上下分隔条** - 拖动调节工作区和Merged Results高度

---

## 性能提示

### 大批量导入建议

如果你有非常多的文件夹（如96个，每个10个文件）：

1. **分批导入** - 可以先导入一部分测试
2. **监控日志** - 查看Help → Show Log了解导入进度
3. **等待完成** - 大批量导入需要时间，请耐心等待

### 导入进度查看

在日志中会看到：
```
✓ Imported: K2023-0460 → light.svs
✓ Imported: K2023-0460 → qp1_480_all.svs
Folder K2023-0460: 9 succeeded, 0 failed
✓ Imported: K2023-1974 → light.svs
...
```

---

## 常见问题

### Q1: 导入后图像显示"Image null"？

**A:** 可能的原因：
1. 图像格式不被支持
2. 文件损坏
3. 文件路径问题

**解决方法：**
- 查看日志找到具体错误
- 尝试手动导入单个文件测试
- 确认文件格式正确

### Q2: 树形结构没有显示？

**A:** 
- 确保重新启动了HPath
- 检查是否已设置"Sort by → Folder"
- 尝试刷新Project界面

### Q3: JSON文件没有导入？

**A:**
- 检查文件扩展名是否为 `.json` 或 `.geojson`
- 查看右侧Objects树形列表
- 检查日志是否有错误信息

### Q4: 如何清除所有导入的内容？

**A:**
- Images: 在树中选择文件夹 → 右键 → Remove images
- Objects: 在树中选择文件 → 右键 → Remove Selected
- 或重新创建Project

---

## 完整功能清单 🎉

### ✅ 所有已实现的功能

**导入功能：**
1. ✅ 单个图像导入
2. ✅ 批量图像导入
3. ✅ 批量文件夹导入（新）
4. ✅ 拖放导入支持

**树形显示：**
5. ✅ Images按文件夹树形显示（新）
6. ✅ Objects按文件夹树形显示（新）
7. ✅ 可折叠/展开

**配对合并：**
8. ✅ 顺序配对（Ctrl+Click）
9. ✅ 正则表达式批量配对
10. ✅ 智能名称匹配

**界面布局：**
11. ✅ 左右可调节分隔条
12. ✅ 上下可调节分隔条（新）
13. ✅ 自适应布局

**数据处理：**
14. ✅ JSON label → classification自动识别
15. ✅ 文件名自动设置（新）
16. ✅ 文件夹元数据自动添加（新）

**多选操作：**
17. ✅ 所有列表支持Shift/Ctrl多选
18. ✅ 批量删除
19. ✅ 批量操作

---

## 更新日期

**2025-10-27**

批量导入文件夹功能已完成，立即可用！🚀

