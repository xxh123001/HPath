# 标注配准功能说明

## 功能简介

当你的标注（annotations）与图像（image）不对齐时，可以使用这个功能通过标记控制点对来自动计算并应用仿射变换，使所有标注与图像对齐。

这个功能支持：
- ✅ 缩放（Scaling）
- ✅ 旋转（Rotation）
- ✅ 平移（Translation）
- ✅ 剪切（Shear）
- ✅ 任意仿射变换组合

## 使用步骤

### 1. 启动功能

在HPath/QuPath菜单中选择：
```
Objects -> Annotations -> Register annotations...
```

或者使用快捷键（如果已配置）。

### 2. 控制面板

启动后会弹出控制面板，显示以下按钮：

- **Add Image Point**: 在图像上标记控制点
- **Add Annotation Point**: 在标注上标记对应的控制点
- **Clear All Points**: 清除所有已标记的点
- **Apply Transform**: 应用变换到所有标注
- **Cancel**: 取消并关闭

### 3. 标记控制点

#### 标记第一个控制点对：

1. 点击 "Add Image Point" 按钮
2. 在**图像上**点击一个特征点（比如某个明显的标记点1）
   - 会显示蓝色圆圈标记
3. 点击 "Add Annotation Point" 按钮
4. 在**标注上**点击对应的点（标注中的点1）
   - 会显示红色圆圈标记
   - 蓝点和红点之间会有绿色连线

#### 继续标记更多控制点对：

重复上述步骤，标记第2、第3个点对...

- 最少需要 **3对控制点** 才能计算变换
- 建议使用 **4-6对控制点** 以获得更好的精度
- 如果有超过3对点，系统会使用最小二乘法计算最优变换

### 4. 选点技巧

为了获得最佳配准效果：

1. **选择明显的特征点**：
   - 血管交叉点
   - 组织边界角点
   - 特殊标记
   
2. **点要分散**：
   - 不要所有点都集中在一个区域
   - 尽量覆盖整个图像范围
   - 四个角落各选一个点效果更好

3. **避免共线点**：
   - 不要所有点在一条直线上
   - 应该形成一个多边形分布

### 5. 应用变换

当标记了至少3对控制点后：

1. 点击 "Apply Transform" 按钮
2. 系统会询问确认
3. 点击 "Yes" 后，变换会应用到**所有标注对象**
4. 变换信息会记录到日志和工作流中

### 6. 取消操作

如果想取消：
- 点击 "Cancel" 按钮，或
- 按 ESC 键，或
- 直接关闭控制面板

## 可视化说明

在选点过程中，viewer中会显示：

- 🔵 **蓝色圆圈** = 图像上的控制点
- 🔴 **红色圆圈** = 标注上的控制点  
- 🟢 **绿色连线** = 对应点对之间的连接
- **数字标签** = 点的序号（1, 2, 3...）

## 技术原理

### 仿射变换

本功能使用仿射变换矩阵：

```
[x']   [a  b  tx]   [x]
[y'] = [c  d  ty] × [y]
[1 ]   [0  0  1 ]   [1]
```

其中：
- `a, d`: 缩放系数
- `b, c`: 剪切/旋转系数
- `tx, ty`: 平移量

### 计算方法

1. **3对点**：直接解线性方程组
2. **4对或更多点**：使用最小二乘法（Least Squares）求最优解

最小二乘法能够处理：
- 测量误差
- 标点不准确
- 获得更稳定的变换

## 常见问题

### Q: 为什么需要至少3对点？

A: 仿射变换有6个参数（a, b, c, d, tx, ty），每对点提供2个方程（x和y方向），所以至少需要3对点（6个方程）才能求解。

### Q: 如果标注太多，会不会很慢？

A: 不会。变换计算非常快速，即使有成千上万个标注，应用变换也只需要几秒钟。

### Q: 可以撤销变换吗？

A: 如果还没有保存项目，可以使用HPath的Undo功能。否则需要重新导入原始标注。

### Q: 变换会保留标注的其他属性吗？

A: 会的！变换只改变标注的几何形状（位置、大小、旋转），不会影响分类、测量值、名称等属性。

### Q: 如果点标错了怎么办？

A: 点击 "Clear All Points" 清除所有点，重新标记。或者按ESC取消当前正在标记的点。

## 工作流集成

应用变换后，以下信息会被记录：

1. **日志**：变换矩阵参数会输出到日志
2. **工作流**：可重现的脚本会添加到历史记录
3. **标注数量**：会显示变换了多少个标注

示例工作流脚本：
```groovy
import qupath.lib.awt.common.AffineTransforms
def transform = AffineTransforms.fromRows(1.0, 0.0, 10.0, 0.0, 1.0, 20.0)
transformAllObjects(transform)
```

## 注意事项

⚠️ **重要提示**：

1. 变换会应用到**当前图像的所有标注**
2. 如果标注已经对齐，不要使用此功能
3. 建议在应用前保存项目，以便出错时恢复
4. 对于大型项目，建议先在副本上测试

## 示例场景

### 场景1：导入的标注比例不对

你从其他软件导入的JSON标注，但图像尺寸不匹配：

1. 找3-4个明显的特征点
2. 在图像上标记这些点
3. 在标注上标记对应点
4. 应用变换

### 场景2：标注旋转了角度

扫描时图像被旋转了：

1. 选择图像四个角附近的特征点
2. 分别在图像和标注上标记
3. 系统会自动计算旋转角度并应用

### 场景3：标注有偏移

标注整体偏移了一段距离：

1. 至少选择3个分散的点
2. 标记对应关系
3. 系统会计算平移量并修正

## 技术支持

如果遇到问题：

1. 查看日志文件（Help -> Show log）
2. 确认至少有3对控制点
3. 确认点的分布合理（不共线）
4. 尝试增加控制点数量以提高精度

---

**版本**: 1.0.0  
**作者**: HPath Team  
**最后更新**: 2025-01

