# 错误修复说明

## 🐛 发现的问题

**错误信息**:
```
Button.text : A bound value cannot be set.
at DefaultScriptEditor.createRunPane(DefaultScriptEditor.java:1014)
```

**原因**:
ActionTools.createButton() 创建的按钮的text属性已经绑定到Action的text，不能再手动设置。

## ✅ 已修复

**修改**: 移除了 `btnStop.setText("⏹ Stop");` 这一行

**现在的代码**:
```java
var btnStop = ActionTools.createButton(killRunningScriptAction);
btnStop.setPadding(new Insets(0, 10, 0, 10));
// 按钮文本会自动使用Action的text
```

## 🧪 验证

重新编译测试应该没有错误了：

```bash
cd qupath-0.6.0
./gradlew build
./build/scripts/HPath
```

停止按钮现在会显示Action的默认文本（"Kill running script"或类似文本）。

**已修复！** ✅

