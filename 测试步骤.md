# JSON 转 GeoJSON 功能测试步骤

## 快速测试步骤

### 1. 启动 HPath

```bash
cd /Users/felix/Downloads/HPath-main/qupath-0.6.0
./gradlew run
```

或者使用已构建的应用：

```bash
cd /Users/felix/Downloads/HPath-main/qupath-0.6.0/build/install/HPath/bin
./HPath
```

### 2. 准备测试环境

1. 启动 HPath 应用
2. 打开任意图像文件（必须先打开图像才能导入对象）
   - `File → Open...` 或直接拖放图像文件

### 3. 测试 JSON 拖放功能

#### 测试 1: 简单多边形
1. 找到测试文件：`example-json-files/sample-polygon.json`
2. 将文件拖放到 HPath 主窗口
3. 应该看到提示对话框询问是否导入对象
4. 点击 "Yes" 导入
5. 检查图像上是否显示了一个矩形多边形

#### 测试 2: 单点
1. 拖放 `example-json-files/sample-point.json`
2. 确认导入后在坐标 (150, 250) 处显示一个点

#### 测试 3: 多对象
1. 拖放 `example-json-files/sample-multiple-objects.json`
2. 应该导入两个多边形对象
3. 检查对象列表中是否有两个新对象

#### 测试 4: 标准 GeoJSON
1. 拖放 `example-json-files/sample-geojson.json`
2. 这个文件已经是 GeoJSON 格式，应该直接解析
3. 查看日志确认显示 "Detected GeoJSON format"

### 4. 验证功能

检查以下几点：

✅ **导入成功**
- 对象出现在图像上
- 对象列表中显示新对象
- 没有错误提示

✅ **属性保留**
- 选择导入的对象
- 在属性面板查看是否保留了原始 JSON 中的元数据

✅ **日志信息**
- 打开日志查看器：`View → Show Log`
- 查找 "Successfully converted and parsed" 消息
- 确认显示正确的对象数量

### 5. 查看日志

关键日志消息：
```
INFO: Detected GeoJSON format, parsing objects...
INFO: Successfully converted and parsed X objects
INFO: Attempting to convert JSON to GeoJSON format...
```

错误日志（如果有问题）：
```
WARN: Could not convert JSON to GeoJSON format
ERROR: Error parsing JSON/GeoJSON
```

## 预期结果

| 测试文件 | 预期对象数 | 几何类型 |
|---------|-----------|---------|
| sample-polygon.json | 1 | Polygon |
| sample-point.json | 1 | Point |
| sample-multiple-objects.json | 2 | Polygon |
| sample-geojson.json | 1 | Polygon |

## 自定义测试

### 创建您自己的 JSON 文件

最简单的格式：
```json
{
  "points": [
    {"x": 10, "y": 10},
    {"x": 50, "y": 10},
    {"x": 50, "y": 50},
    {"x": 10, "y": 50},
    {"x": 10, "y": 10}
  ]
}
```

拖放到 HPath 测试！

## 故障排查

### 问题: 拖放没有反应
**解决**: 
- 确认已打开图像
- 检查文件扩展名是 `.json`
- 查看日志是否有错误

### 问题: 导入后看不到对象
**解决**:
- 检查坐标值是否在图像范围内
- 缩放查看整个图像
- 检查对象列表确认对象已添加

### 问题: 转换失败
**解决**:
- 验证 JSON 格式正确
- 确保包含坐标数据字段
- 查看详细错误日志

## 开发测试

如果修改了代码，重新构建：

```bash
cd /Users/felix/Downloads/HPath-main/qupath-0.6.0
./gradlew build -x test
./gradlew run
```

## 成功标准

✅ 所有 4 个测试文件都能成功导入  
✅ 对象正确显示在图像上  
✅ 属性数据完整保留  
✅ 日志中没有错误消息  
✅ 可以正常编辑导入的对象  

---

**注意**: 如果遇到任何问题，请查看 `JSON转GeoJSON功能说明.md` 获取详细信息。

