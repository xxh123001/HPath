# 如何查看 JSON 导入日志

## 方法1: 在 HPath 界面查看

1. 启动 HPath
2. 点击菜单 `View → Show Log`
3. 拖放你的 GeoJSON 文件
4. 在日志窗口中查找以下关键信息：

### 期待看到的日志：

```
INFO: JsonToGeoJsonHandler.handleDrop called with 1 elements
INFO: Processing element 1 of 1
INFO: Detected GeoJSON format, parsing objects...
INFO: Successfully parsed XXX PathObjects from GeoJSON
INFO: Total objects collected: XXX
INFO: Prompting to import XXX objects
```

### 如果出错会看到：

```
ERROR: Error parsing GeoJSON: <具体错误信息>
ERROR: JSON parsing error: <具体错误信息>
WARN: No active viewer or image data available for JSON import
```

## 方法2: 在终端查看（如果你用 gradlew run 启动）

所有日志会直接输出到终端，更加详细。

## 常见问题排查

### 1. 看到 "Reduction failed, possible invalid input"
**原因**: GeoJSON 中的多边形环没有正确闭合
**解决**: 代码已经自动处理，但如果还有问题请检查 JSON 文件

### 2. 看到 "No active viewer or image data available"
**原因**: 没有打开图像
**解决**: 必须先打开一个图像文件！

### 3. 没有任何日志输出
**原因**: Handler 没有被调用
**可能**: 
- 文件不是 .json 扩展名
- DragDropImportListener 没有正确注册 handler

## 测试步骤

1. ✅ 启动 HPath
2. ✅ 打开 `View → Show Log` 
3. ✅ 打开一个测试图像（任意图像）
4. ✅ 拖放你的 GeoJSON 文件：
   `/Users/felix/Library/Containers/com.tencent.xinWeChat/Data/Documents/xwechat_files/wxid_kjh0q6eeyhhv12_628b/msg/file/2025-10/annotation_batch_002_of_129.geojson`
5. ✅ 观察日志输出

## 当前代码添加的详细日志：

```java
// 入口日志
logger.info("JsonToGeoJsonHandler.handleDrop called with {} elements", elements.size());

// 每个元素处理
logger.info("Processing element {} of {}", i + 1, elements.size());

// GeoJSON 检测
logger.info("Detected GeoJSON format, parsing objects...");
logger.info("Successfully parsed {} PathObjects from GeoJSON", objects.size());

// 转换逻辑
logger.info("Element is not GeoJSON, attempting to convert...");
logger.info("Successfully converted to GeoJSON, now parsing...");

// 汇总
logger.info("Total objects collected: {}", allObjects.size());
logger.info("Prompting to import {} objects", allObjects.size());

// 错误
logger.error("Error parsing GeoJSON: {}", e.getMessage(), e);
logger.error("Unexpected error during JSON/GeoJSON handling: {}", e.getMessage(), e);
```

---

**如果日志中没有 "JsonToGeoJsonHandler.handleDrop called"，说明 handler 根本没有被调用！**

这可能意味着：
1. DragDropImportListener 的 JSON handler 列表为空
2. 文件没有被识别为 JSON
3. 其他 handler 先处理了这个文件

