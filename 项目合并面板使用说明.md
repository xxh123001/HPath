# 项目合并面板（Project Merger）使用说明

## ✨ 功能概述

**项目合并面板**让你可以分别导入图像和标注文件，然后通过勾选组合的方式创建配对，解决了传统拖放方式的不便。

---

## 🎯 界面布局

```
┌────────────────────────────────────────────────────────┐
│  Project Merger                                        │
├────────────────────┬───────────────────────────────────┤
│ 📷 Images          │ 📝 Objects (JSON/GeoJSON)        │
├────────────────────┼───────────────────────────────────┤
│ [Import Images...] │ [Import Objects...]               │
│ 🔍 Search...       │ 🔍 Search...                     │
├────────────────────┼───────────────────────────────────┤
│ ☑ image1.svs       │ ☑ objects1.geojson (100 obj)     │
│ ☐ image2.svs       │ ☑ objects2.geojson (200 obj)     │
│ ☑ image3.tif       │ ☐ objects3.json (50 obj)         │
│ ...                │ ...                               │
├────────────────────┴───────────────────────────────────┤
│               [⬇ Merge Selected]                       │
├────────────────────────────────────────────────────────┤
│ 📊 Merged Results                                      │
├────────────────────────────────────────────────────────┤
│ image1.svs + objects1.geojson (100 objects)           │
│ image1.svs + objects2.geojson (200 objects)           │
│ image3.tif + objects1.geojson (100 objects)           │
│ image3.tif + objects2.geojson (200 objects)           │
│ [Open Selected] [Remove] [Clear All]                   │
└────────────────────────────────────────────────────────┘
```

---

## 🚀 使用流程

### Step 1: 打开面板

```
Project → Project Merger...
```

或使用快捷键（如果配置）

### Step 2: 导入图像（左侧）

1. **点击 "Import Images..." 按钮**
2. **选择一个或多个图像文件**
   - 支持：.svs, .tif, .ndpi, .vsi, .mrxs等
3. **图像出现在列表中，带复选框**

### Step 3: 导入对象（右侧）

1. **点击 "Import Objects..." 按钮**
2. **选择一个或多个JSON/GeoJSON文件**
3. **对象文件出现在列表中，显示对象数量**

### Step 4: 搜索（可选）

**在图像列表中**:
- 点击图像列表
- 按 **Ctrl+F** (Mac: Cmd+F)
- 搜索框自动聚焦
- 输入关键词过滤

**在对象列表中**:
- 点击对象列表
- 按 **Ctrl+F**
- 搜索对象文件名

### Step 5: 勾选要合并的项

**图像列表**:
- ☑ 勾选想要的图像（可多选）

**对象列表**:
- ☑ 勾选想要的对象文件（可多选）

**快捷操作**:
- "Select All" - 全选
- "Clear" - 全不选

### Step 6: 合并

**点击中间的 "⬇ Merge Selected" 按钮**

**结果**:
- 每个选中的图像 × 每个选中的对象 = 一个合并项
- 例如：2个图像 × 3个对象 = 6个合并项

### Step 7: 打开合并的项目

**在下方的Merged Results列表**:
- **双击**任意合并项
- 或选中后点击 "Open Selected"

**效果**:
- ✅ 自动打开图像
- ✅ 自动导入对象
- ✅ 立即可用

---

## 💡 使用示例

### 示例1: 一个图像配一个对象

**场景**: 你有配对的图像和标注文件

```
Step 1: 导入
  图像: sample1.svs
  对象: sample1_annotations.geojson
  
Step 2: 勾选
  ☑ sample1.svs
  ☑ sample1_annotations.geojson
  
Step 3: Merge
  → 创建: sample1.svs + sample1_annotations.geojson
  
Step 4: 双击打开
  → 图像和标注一起加载！
```

### 示例2: 一个图像配多个对象

**场景**: 同一个图像有不同的标注版本

```
Step 1: 导入
  图像: slide.svs
  对象: manual_annotations.json
       ai_annotations.json
       corrected_annotations.json
  
Step 2: 勾选
  ☑ slide.svs
  ☑ manual_annotations.json
  ☑ ai_annotations.json
  
Step 3: Merge
  → 创建3个合并项
  
Step 4: 双击选择要打开的版本
```

### 示例3: 批量处理

**场景**: 多个图像用同一套标注

```
Step 1: 导入
  图像: slide1.svs, slide2.svs, slide3.svs
  对象: template.geojson
  
Step 2: 勾选
  ☑ slide1.svs
  ☑ slide2.svs  
  ☑ slide3.svs
  ☑ template.geojson
  
Step 3: Merge
  → 创建3个合并项（每个图像+template）
  
Step 4: 逐个打开处理
```

---

## ⌨️ 快捷键

| 操作 | 快捷键 |
|------|--------|
| 在图像列表搜索 | Ctrl+F (Cmd+F) |
| 在对象列表搜索 | Ctrl+F (Cmd+F) |
| 打开合并项 | 双击 |

---

## 🔍 搜索功能

### 图像搜索

**搜索范围**:
- 文件名

**示例**:
```
输入: slide
结果: slide1.svs, slide2.svs, my_slide.tif

输入: .svs
结果: 所有.svs文件

输入: 2025
结果: 文件名包含2025的图像
```

### 对象搜索

**搜索范围**:
- 文件名

**示例**:
```
输入: annotations
结果: manual_annotations.json, ai_annotations.geojson

输入: tumor
结果: tumor_regions.json
```

---

## 📊 合并逻辑

### 合并规则

**笛卡尔积**:
```
选中图像数: m
选中对象数: n
合并项数: m × n
```

**例子**:
```
图像: [A, B]
对象: [X, Y, Z]

合并结果:
1. A + X
2. A + Y
3. A + Z
4. B + X
5. B + Y
6. B + Z

总共: 2 × 3 = 6 项
```

---

## 🎯 实际应用场景

### 场景1: 配对的图像和标注

```
目录结构:
/data/
├── slide_001.svs
├── slide_001.geojson
├── slide_002.svs
├── slide_002.geojson
└── ...

操作:
1. 导入所有图像
2. 导入所有GeoJSON
3. 勾选 slide_001.svs + slide_001.geojson
4. Merge → 双击打开
```

### 场景2: 标注对齐测试

```
同一张图像，多个标注版本:
├── image.svs
├── version1.json
├── version2.json
└── corrected.json

操作:
1. 导入image.svs
2. 导入3个JSON
3. 勾选所有
4. Merge → 得到3个合并项
5. 逐个双击对比效果
```

### 场景3: 批量导入项目

```
新项目，10张切片，每张都有标注:
├── images/ (10个.svs)
└── annotations/ (10个.geojson)

操作:
1. Select All images
2. Select All objects  
3. Merge → 得到100个组合
4. 根据需要打开特定组合
```

---

## ⚠️ 注意事项

### 文件格式

**图像支持**:
- .svs (Aperio)
- .tif, .tiff (TIFF)
- .ndpi (Hamamatsu)
- .vsi (Olympus)
- .mrxs (3DHISTECH)
- .scn (Leica)
- 其他QuPath支持的格式

**对象支持**:
- .json (JSON)
- .geojson (GeoJSON)
- 必须是QuPath导出的格式

### 性能提示

- **大量对象**: 导入1000+个对象的文件可能需要几秒
- **搜索**: 搜索是即时的，无性能问题
- **合并**: 只创建引用，不消耗大量内存

---

## 🛠️ 菜单集成

**位置**: `Project → Project Merger...`

**或者**:
- File菜单（根据最终集成位置）
- 工具栏按钮（可添加）

---

## 🎊 优势对比

### vs 传统拖放方式

| 特性 | 传统拖放 | 项目合并面板 |
|------|---------|-------------|
| 同时看到图像和对象 | ❌ | ✅ |
| 批量选择 | ❌ | ✅ |
| 搜索 | ❌ | ✅ Ctrl+F |
| 多对多组合 | ❌ 困难 | ✅ 一键 |
| 预览对象数量 | ❌ | ✅ |
| 管理合并项 | ❌ | ✅ |

---

## 📋 完整工作流程

```
1. 打开 Project Merger
   ↓
2. 左侧导入图像（可多个）
   ↓
3. 右侧导入对象文件（可多个）
   ↓  
4. 勾选想要的图像（支持搜索Ctrl+F）
   ↓
5. 勾选想要的对象（支持搜索Ctrl+F）
   ↓
6. 点击 Merge Selected
   ↓
7. 下方显示合并结果
   ↓
8. 双击打开想要的组合
   ↓
9. 图像和标注自动加载！✅
```

---

## 🚀 立即使用

### 编译

```bash
cd /Users/felix/Downloads/HPath-main/qupath-0.6.0
./gradlew build
./build/scripts/HPath
```

### 菜单位置

```
Project → Project Merger...
```

### 快速测试

1. 打开面板
2. 导入一张图像
3. 导入一个GeoJSON
4. 勾选两个
5. 点击Merge
6. 双击合并项
7. 验证自动加载

---

**完美解决大量图像+标注的导入问题！** 🎯

**特别适合批量项目管理！** 🚀

