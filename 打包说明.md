# HPath DMG 打包说明

## 🎁 正在打包...

当前正在执行：
```bash
./gradlew jpackage -Pqupath.package=dmg
```

## 📦 打包过程

打包可能需要 **5-15 分钟**，包括：

1. ✅ 编译所有模块
2. ✅ 创建 Java Runtime
3. ✅ 使用 jpackage 创建应用程序镜像
4. ✅ 生成 DMG 安装包
5. ✅ 计算校验和

## 📍 输出位置

DMG 文件将保存在：
```
/Users/felix/Downloads/HPath-main/qupath-0.6.0/build/dist/
```

文件名类似：
```
HPath-1.0.0.dmg
```

## 🔍 如何查看进度

打包过程中会显示：
```
> Task :jpackageImage
> Task :jpackage
> Task :jpackageFinalize
```

## ⚡ 其他打包选项

### 只创建应用程序镜像（不打包 DMG，更快）
```bash
./gradlew jpackageImage
```
输出：`build/dist/HPath.app`

### 创建所有类型的安装包
```bash
./gradlew jpackage -Pqupath.package=all
```
会创建：`.app`, `.dmg`, `.pkg`

### 只创建 PKG 安装包
```bash
./gradlew jpackage -Pqupath.package=pkg
```

## ✨ 已包含的功能修改

打包的 DMG 将包含：

### 1. JSON 到 GeoJSON 自动转换
- 拖放 JSON 文件自动转换为 GeoJSON
- 支持多种 JSON 格式

### 2. GeoJSON 导入增强
- 修复了精度降低错误
- 保留高精度几何图形
- 详细的导入日志

### 3. 开挂模式：自动 Annotation
- **所有 detection 对象自动导入为 annotation**
- 直接在 Annotation list 中显示
- 无需手动转换

## 📋 打包完成后

### 安装
1. 双击 `.dmg` 文件
2. 将 HPath 拖到 Applications 文件夹
3. 启动 HPath

### 测试
1. 打开图像
2. 拖放你的 GeoJSON 文件
3. 查看 Annotation list
4. 应该看到 1000 个对象！

## 🛠️ 故障排查

### 打包失败
- 确保有足够的磁盘空间（至少 5GB）
- 检查终端错误信息

### DMG 文件太大
- 正常，包含完整的 Java Runtime
- 大小约 300-500 MB

### macOS 安全警告
- 首次打开时右键点击 → "打开"
- 或在系统偏好设置中允许

## 📁 清理

如果需要清理构建文件：
```bash
cd /Users/felix/Downloads/HPath-main/qupath-0.6.0
./gradlew clean
```

---

**打包正在进行中，请耐心等待...**

构建完成后会显示：
```
BUILD SUCCESSFUL
```

然后在 `build/dist/` 目录找到你的 DMG 文件！ 🎉

